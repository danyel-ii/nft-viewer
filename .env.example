# Required: Alchemy API key (server-side only)
# Used for Alchemy NFT API calls like:
# https://eth-mainnet.g.alchemy.com/nft/v3/<ALCHEMY_KEY>/getNFTsForOwner
ALCHEMY_KEY=

# Optional (recommended) for ENS resolution: Ethereum mainnet RPC HTTP URL (server-side only)
# If omitted, the server will fall back to using ALCHEMY_KEY to build a mainnet RPC URL.
RPC_URL=
